{
  "name": "mizani",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd mizani-frontend && npm run dev",
    "build": "cd mizani-frontend && npm run build",
    "deploy:functions": "node scripts/deploy-functions.js",
    "deploy:frontend": "npm run build && vercel --prod",
    "install:all": "cd mizani-frontend && npm install && cd ../mizani-backend/functions/config && npm install && cd ../score-csv && npm install && cd ../generate-report && npm install && cd ../onchain-payments && npm install"
  }
}
